
# In lieu of setting a million symlinks up in bin/ or ~/.local/bin, using aliases for one-off programs instead.

if [ -x "$HOME/.local/blender/blender" ]; then

    alias blender="$HOME/.local/blender/blender"

fi
