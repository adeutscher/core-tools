# This is an example file to confirm the environment variables that
#     can be used to edit per-host definitions.
# Reminder: Per-host definitions are processed after other function files.

# If BASIC_PROMPT has content, stick to an instant prompt with zero colours or calculations.
# BASIC_PROPT=1

# Use CONKY_SCREEN to have conky start up in the bottom-right of a particular display
#     instead of the primary or first one.
# Screen names can be confirmed with xrandr: xrandr --current
# CONKY_SCREEN=DVI0

# Use RDP_WIDTH and RDP_HEIGHT to control the width of the RDP window conjured by the 'rdp' function.
# RDP_WIDTH=1600
# RDP_HEIGHT=900

